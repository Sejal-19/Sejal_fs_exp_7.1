# React + Express Axios Connection

## 🎯 Objective
Learn how to connect a React frontend to an Express.js backend using Axios for data fetching.

## 🧩 Description
This project demonstrates how to build a simple Express API that provides product data and a React frontend that fetches and displays that data using Axios.

## 🛠️ Steps to Run

### 1️⃣ Backend Setup
1. Navigate to the `backend` folder.
2. Install dependencies:
   ```bash
   npm install
Run the server:

node server.js


API will be available at http://localhost:5000/api/products

2️⃣ Frontend Setup

Navigate to the frontend folder.

Install dependencies:

npm install


Start the React app:

npm start


The app will run at http://localhost:3000
 and fetch data from the backend.

⚙️ Tech Stack

React

Axios

Express.js

Node.js

📸 Expected Output

A web page displaying a product list with names and prices fetched from the Express API.
